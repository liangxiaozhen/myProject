package com.ptpl.service;

import com.ptpl.model.DebtAttornBuyer;

import java.math.BigDecimal;
import java.util.List;

/**
 * @author liuj
 * 标的债权转让购买人设置service层
 */
public interface DebtAttornBuyerServiceI extends BaseService<DebtAttornBuyer>{
	/**
	 * 根据tid查询购买人的相关设置
	 * @param @param tid
	 * @param @return
	 * @return DebtAttornBuyer
	 * @author jiangxueyou
	 */
	DebtAttornBuyer selectByTid(BigDecimal tid);

	List<DebtAttornBuyer> selectListByTid(BigDecimal tid);
}
