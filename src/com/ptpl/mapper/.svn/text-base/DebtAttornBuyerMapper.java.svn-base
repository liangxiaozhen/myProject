package com.ptpl.mapper;

import com.ptpl.model.DebtAttornBuyer;

import java.math.BigDecimal;
import java.util.List;
/**
 * @author liuj
 * 标的债权转让购买人设置表
 */
public interface DebtAttornBuyerMapper extends BaseMapper<DebtAttornBuyer>{
	/**
	 * 根据tid查询购买人的相关设置
	 * @param @param tid
	 * @param @return
	 * @return DebtAttornBuyer
	 * @author jiangxueyou
	 */
	DebtAttornBuyer selectByTid(BigDecimal tid);

    List<DebtAttornBuyer> selectListByTid(BigDecimal tid);
}